import { HabilidadesCategoria } from './habilidades-categoria';

describe('HabilidadesCategoria', () => {
  it('should create an instance', () => {
    expect(new HabilidadesCategoria()).toBeTruthy();
  });
});
