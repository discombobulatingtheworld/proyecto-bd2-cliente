import { PasswordCambioRealizacion } from './password-cambio-realizacion';

describe('PasswordCambioRealizacion', () => {
  it('should create an instance', () => {
    expect(new PasswordCambioRealizacion()).toBeTruthy();
  });
});
