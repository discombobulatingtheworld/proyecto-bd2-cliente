import { InputDateFormatDirective } from './input-date-format.directive';

describe('InputDateFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDateFormatDirective();
    expect(directive).toBeTruthy();
  });
});
